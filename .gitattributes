# SCM syntax highlighting & preventing 3-way merges
pixi.lock merge=binary linguist-language=YAML linguist-generated=true -diff
# Project modification: publish trained checkpoints through Git LFS.
AIC_SUBMISSION/models/*.pt filter=lfs diff=lfs merge=lfs -text
